# Custom Avatar Pictures Folder

Drop any profile picture files (`.png`, `.jpg`, `.jpeg`, `.webp`, `.svg`) into this folder (`public/avatars/`).

They will automatically be available as preset avatar choices in your Patron Ticket profile customization menu!
